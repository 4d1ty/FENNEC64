# FENNEC64
A virtual machine with custom instruction sets


Its weird, its called `FENNEC64`, but it's a 32-bit architecture. Don't ask why.


```
make yip && build\yip example\stack.fnc stack.bin && make vixen && build\vixen stack.bin -d
```